import model from 'sportsbook/model/bets/SelectedBetsModel'

describe('SelectedBetsModel', () => {
    describe('notify', () => {
        it('should trigger x number of CHANGE_BETS events', () => {
            // model.Bets = {a: 1, b: 2, c: 3}

        })
    });
});
